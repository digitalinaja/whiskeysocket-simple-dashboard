# Application Environment
NODE_ENV=production

# Application Name (default: Alhikmah CRM)
APP_NAME=Alhikmah CRM

# Server Port (default: 3000)
PORT=3000

# JWT Authentication (for SSO integration)
# Secret key untuk validasi JWT token dari external SSO
JWT_SECRET=your-sso-jwt-secret-key-here

# Nama cookie untuk menyimpan JWT token (default: sso_token)
JWT_COOKIE_NAME=sso_token

# External SSO Login Page URL
# User akan di-redirect ke URL ini jika belum login
SSO_LOGIN_URL=https://sso.example.com/login

# Redirect URL setelah berhasil login di SSO
# SSO akan redirect kembali ke aplikasi dengan JWT token
SSO_REDIRECT_URL=https://your-app.com
